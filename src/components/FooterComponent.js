import React from "react";
import {Layout} from "antd";

function FooterComponent() {
    const {Footer} = Layout;
    return <Footer style={{textAlign: 'center'}}>通用审核后台 ©2020 Created</Footer>
}

export default FooterComponent;
